import React from 'react';
import {Button, Text} from 'native-base';
import {StyleSheet} from 'react-native';

module.exports = {
  //////////////////////////////////////////////////////////////////////////////////////////////////
  //
  //////////////////////////////////////////////////////////////////////////////////////////////////
  SwitchOn: (props) => {
    return (
      <Button rounded style={styles.switchStyle} onPress={props.onSwitchOn}>
        <Text> ON </Text>
      </Button>
    );
  },
  //////////////////////////////////////////////////////////////////////////////////////////////////
  //
  //////////////////////////////////////////////////////////////////////////////////////////////////
  SwitchOff: (props) => {
    return (
      <Button rounded style={styles.switchStyle} onPress={props.onSwitchOff}>
        <Text> OFF </Text>
      </Button>
    );
  },
};

////////////////////////////////////////////////////////////////////////////////////////////////////
//
////////////////////////////////////////////////////////////////////////////////////////////////////
const styles = StyleSheet.create({
  switchStyle: {
    backgroundColor: '#614031',
    color: '#b2b2b2',
    alignSelf: 'center',
    margin: 0,
  },
});
